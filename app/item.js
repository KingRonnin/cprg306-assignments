function item({name, quantity, category}) {
    return (
    <section>
        <ul>
            <li>Name: {name}</li>
            <li>Quantity: {quantity}</li>
            <li>Category: {category}</li>
        </ul>
    </section>
    );
}